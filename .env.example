# Public Square-hosted checkout URL. This is safe to expose in the built site.
VITE_SQUARE_CHECKOUT_URL=https://square.link/u/REPLACE_ME

# Used only by `npm run square:create-link`. Never expose either value to Vite.
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENV=production
